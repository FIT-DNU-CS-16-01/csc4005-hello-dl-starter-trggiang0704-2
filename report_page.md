# Report Page - Lab0

Tôi đã thiết lập môi trường học phần bằng Anaconda bằng cách tạo environment riêng với Python 3.10 nhằm đảm bảo tính ổn định và tránh xung đột thư viện. Sau đó, tôi cập nhật pip và cài đặt các thư viện cần thiết từ file requirements.txt.

Tôi tiến hành kiểm tra môi trường bằng cách chạy check_env.py để xác nhận các thư viện và cấu hình đã được cài đặt đúng. Tiếp theo, tôi chạy run_smoke_test.py để kiểm tra pipeline cơ bản của hệ thống.

Trong quá trình thực hiện, tôi gặp một số vấn đề nhỏ liên quan đến thiếu thư viện, đã được khắc phục bằng cách cài đặt lại theo requirements.txt.

Kết quả, hệ thống đã sinh ra đầy đủ các file output yêu cầu như log kiểm tra môi trường, log chạy smoke test, biểu đồ loss và file checkpoint của mô hình, chứng tỏ pipeline hoạt động chính xác.
